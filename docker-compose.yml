services:
  mathkids:
    build: .
    container_name: mathkids
    restart: unless-stopped
    ports:
      - "8002:8000"
    volumes:
      - .:/app
    labels:
      - com.centurylinklabs.watchtower.enable=false
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

